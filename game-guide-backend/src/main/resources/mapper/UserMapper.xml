<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gameguide.mapper.UserMapper">

    <resultMap id="BaseResultMap" type="com.gameguide.entity.User">
        <id column="id" property="id"/>
        <result column="username" property="username"/>
        <result column="password" property="password"/>
        <result column="role" property="role"/>
        <result column="created_at" property="createdAt"/>
    </resultMap>

    <insert id="insert" parameterType="com.gameguide.entity.User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO "user" (username, password, role, created_at)
        VALUES (#{username}, #{password}, #{role}, CURRENT_TIMESTAMP)
    </insert>

    <select id="selectByUsername" resultMap="BaseResultMap">
        SELECT id, username, password, role, created_at
        FROM "user"
        WHERE username = #{username}
    </select>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT id, username, password, role, created_at
        FROM "user"
        WHERE id = #{id}
    </select>

</mapper>



